language: node_js
node_js:
  - "stable"
cache:
  directories:
    - node_modules
script:
  npm run build
deploy:
  provider: pages
  cleanup: true
  local_dir: build
  github_token: $GITHUB_TOKEN
  keep_history: true
  on:
    branch: main