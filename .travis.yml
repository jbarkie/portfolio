language: node_js
node_js:
  - "14"
cache:
  directories:
    - node_modules
script:
  npm run build
deploy:
  provider: pages
  cleanup: true
  local_dir: public
  github_token: $GITHUB_TOKEN
  keep_history: true
  on:
    branch: main